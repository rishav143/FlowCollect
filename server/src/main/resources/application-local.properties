# Local dev overrides — loaded when SPRING_PROFILES_ACTIVE=local
# Overrides the PostgreSQLDialect set in application.properties for local MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
