# Backend API base URL
VITE_API_URL=http://localhost:8080

# Set to "true" to use in-memory mocks instead of real API (UI-only dev)
# Set to "false" (or remove) to hit the real backend
VITE_USE_MOCK=false
